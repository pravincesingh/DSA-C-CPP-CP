#include<stdio.h>
#include<conio.h>
const int size=20;//Global variable so now it can be accessed by entire program
int n=0;


void main()
{
    int choice;
    int arr[10];
    do
    {
        printf("Press 1 for INSERTION_UNSORTED\n);
        printf("Press 2 for INSERTION_SORTED\n")
    }
}
